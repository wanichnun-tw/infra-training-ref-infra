# Stack

### Init

```bash
# terraform init -backend-config="bucket=<bucket_name>" -backend-config="key=<team>/<environment>/<stack>"
terraform init -backend-config="bucket=infra-training-state-2022" -backend-config="key=ankit/dev/terralith"
```
